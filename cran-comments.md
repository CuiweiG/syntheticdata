## R CMD check results
0 errors | 0 warnings | 0 notes

## Test environments
* GitHub Actions: ubuntu-latest, macos-latest, windows-latest (R release)

## Reverse dependencies
New package, no reverse dependencies.

## Package positioning
Existing CRAN packages for synthetic data (synthpop, simPop)
focus on survey/census data synthesis. syntheticdata provides
a lightweight, clinically oriented workflow combining Gaussian
copula generation with integrated privacy-utility validation
metrics (KS statistic, discriminative AUC, nearest-neighbor
distance ratio).
