% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{validate_synthetic}
\alias{validate_synthetic}
\title{Validate synthetic data quality}
\usage{
validate_synthetic(
  x,
  metrics = c("distributional", "correlation", "discriminative", "privacy")
)
}
\arguments{
\item{x}{A \code{synthetic_data} object from \code{\link[=synthesize]{synthesize()}}.}

\item{metrics}{Character vector of metrics:
\itemize{
\item \code{"distributional"}: KS statistic per numeric variable.
\item \code{"correlation"}: Frobenius norm of correlation difference.
\item \code{"discriminative"}: AUC of real-vs-synthetic classifier.
\item \code{"privacy"}: nearest-neighbor distance ratio.
}}
}
\value{
A \code{synthetic_validation} object (tibble) with columns:
\code{metric}, \code{value}, \code{interpretation}.
}
\description{
Computes utility and privacy metrics comparing synthetic data
to the original real dataset.
}
\details{
Utility metrics assess how well the synthetic data preserves
statistical properties. Privacy metrics assess the risk of
re-identification.

Discriminative accuracy near 0.5 means the synthetic data is
indistinguishable from real data. Privacy ratio > 1 means
synthetic records are not closer to real records than real
records are to each other.
}
\examples{
set.seed(42)
real <- data.frame(age = rnorm(100, 65, 10), sbp = rnorm(100, 130, 20))
syn <- synthesize(real, seed = 42)
validate_synthetic(syn)

}
\references{
Snoke J, et al. (2018). General and specific utility measures
for synthetic data. \emph{Journal of the Royal Statistical Society
A}, 181(3):663--688. \doi{10.1111/rssa.12358}
}
