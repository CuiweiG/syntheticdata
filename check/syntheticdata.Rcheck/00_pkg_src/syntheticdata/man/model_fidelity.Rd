% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility.R
\name{model_fidelity}
\alias{model_fidelity}
\title{Downstream model fidelity test}
\usage{
model_fidelity(x, outcome, predictors = NULL)
}
\arguments{
\item{x}{A \code{synthetic_data} object.}

\item{outcome}{Character. Name of the outcome column.}

\item{predictors}{Character vector (optional). Predictor
columns. Default: all other numeric columns.}
}
\value{
A tibble with columns: \code{train_data}, \code{metric}, \code{value}.
For binary outcomes the metric is AUC; for continuous outcomes
it is R-squared.
}
\description{
Trains a predictive model on synthetic data and evaluates it
on real data. Compares to a model trained on real data (gold
standard). Measures whether synthetic data preserves
predictive signal.
}
\examples{
set.seed(42)
real <- data.frame(
    x1 = rnorm(200), x2 = rnorm(200),
    y = rbinom(200, 1, 0.3))
syn <- synthesize(real, seed = 42)
model_fidelity(syn, outcome = "y")
}
